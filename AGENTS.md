# Portal Agent Notes

`CLAUDE.md` is the source of truth for commands, architecture, and agent-facing
project guidance. Read it before making code changes.

This file intentionally stays short so Codex/agent entry points do not drift
from the current R2 architecture.
